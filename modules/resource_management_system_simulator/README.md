# Resource Management System Simulator

This module aims at simulating the resource management system environment. This deploys a VPC containing an instance with an OPC client to monitor OPC tags. A transit gateway attachment is also created to route traffic to the sites.